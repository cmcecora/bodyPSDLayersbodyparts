---
phase: "06-polish-back-view-performance"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/body-map-explorer.ts
  - src/body-map-sidebar.ts
  - src/body-map-detail-panel.ts
  - src/body-map-data-panel.ts
  - src/styles/tokens.css.ts
  - src/__tests__/body-map-explorer.test.ts
autonomous: false
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "The component layout adapts to container width using container queries, not viewport media queries."
    - "Panels, cards, and controls have a more professional visual system with consistent radius, shadow, spacing, and focus treatment."
    - "Narrow layouts remain readable without horizontal overflow."
  artifacts:
    - path: "src/body-map-explorer.ts"
      provides: "Container-query layout and responsive panel ordering"
    - path: "src/styles/tokens.css.ts"
      provides: "Shared polish tokens for surface, shadow, radius, and focus"
    - path: "src/body-map-sidebar.ts"
      provides: "Refined interactive states for the left rail"
    - path: "src/body-map-detail-panel.ts"
      provides: "Polished detail/photo card presentation"
    - path: "src/body-map-data-panel.ts"
      provides: "Responsive 4th-column behavior and polished card styling"
  key_links:
    - from: "explorer layout container"
      to: "sidebar/detail/data panel placement"
      via: "@container rules"
---

<objective>
Replace the current fixed four-column shell with a component-responsive layout and apply a consistent visual polish pass to the explorer’s panels and controls. This plan keeps the responsive behavior scoped to the component itself so the host can embed it at any width without relying on viewport media queries.

Purpose: deliver UX-01 and UX-02 while keeping the back-view and performance work free to focus on the model internals later.
Output: container-query-based layout rules, refined surface and interaction styling, and automated tests that lock the responsive contract into the codebase.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-back-view-performance/06-RESEARCH.md

@src/body-map-explorer.ts
@src/body-map-sidebar.ts
@src/body-map-detail-panel.ts
@src/body-map-data-panel.ts
@src/styles/tokens.css.ts
@src/__tests__/body-map-explorer.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert the explorer shell to a container-query layout</name>
  <files>src/body-map-explorer.ts, src/body-map-data-panel.ts, src/__tests__/body-map-explorer.test.ts</files>
  <read_first>
    - src/body-map-explorer.ts
    - src/body-map-data-panel.ts
    - src/__tests__/body-map-explorer.test.ts
  </read_first>
  <action>
    Replace the fixed explorer shell with concrete container-query breakpoints:
    1. Add `container-type: inline-size` to the host or top-level explorer layout container in `src/body-map-explorer.ts`.
    2. Keep the wide layout as four columns, but add `@container (max-width: 1280px)` to collapse into two columns and `@container (max-width: 820px)` to collapse into a single-column stack.
    3. Use explicit layout areas or ordered grid rows so the narrow layout renders the model first, then sidebar, detail panel, and data panel without horizontal overflow.
    4. In the narrow container rules, disable the data panel’s sticky behavior and max-height viewport lock so it scrolls naturally in document flow.
    5. Add or update tests in `src/__tests__/body-map-explorer.test.ts` that assert the explorer style text contains `container-type: inline-size` and `@container` rules for the chosen breakpoints.
  </action>
  <acceptance_criteria>
    - `src/body-map-explorer.ts` contains `container-type: inline-size`.
    - `src/body-map-explorer.ts` contains `@container (max-width: 1280px)` and `@container (max-width: 820px)`.
    - `src/body-map-data-panel.ts` no longer forces sticky/max-height behavior in the narrow container mode.
    - `src/__tests__/body-map-explorer.test.ts` contains assertions for `container-type: inline-size` and `@container`.
    - `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The explorer shell reflows by container width and does not depend on viewport media queries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply a shared visual polish pass to panels, cards, and focus states</name>
  <files>src/styles/tokens.css.ts, src/body-map-sidebar.ts, src/body-map-detail-panel.ts, src/body-map-data-panel.ts, src/__tests__/body-map-explorer.test.ts</files>
  <read_first>
    - src/styles/tokens.css.ts
    - src/body-map-sidebar.ts
    - src/body-map-detail-panel.ts
    - src/body-map-data-panel.ts
    - src/__tests__/body-map-explorer.test.ts
  </read_first>
  <action>
    Introduce and use concrete polish tokens and styles instead of one-off component values:
    1. In `src/styles/tokens.css.ts`, add shared tokens for panel radius, soft/strong shadow, focus ring color, and panel spacing (for example `--bme-radius-lg`, `--bme-shadow-soft`, `--bme-focus-ring`).
    2. Update sidebar, detail panel, and data panel cards/headers to use those tokens instead of ad-hoc border radius and shadow values.
    3. Add a consistent `:focus-visible` treatment to interactive buttons, cards, and search inputs using the shared focus-ring token.
    4. Refine selected/hover states so active panels and list items have a stronger visual hierarchy than the current flat gray backgrounds.
    5. Add or update a style-contract test in `src/__tests__/body-map-explorer.test.ts` that asserts the new shared tokens and focus-visible styles exist in the rendered CSS text.
  </action>
  <acceptance_criteria>
    - `src/styles/tokens.css.ts` contains new shared polish tokens for radius, shadow, and focus ring.
    - `src/body-map-sidebar.ts`, `src/body-map-detail-panel.ts`, and `src/body-map-data-panel.ts` reference those tokens.
    - At least one of the component style blocks contains `:focus-visible`.
    - `src/__tests__/body-map-explorer.test.ts` contains assertions against the new token or focus-visible CSS text.
    - `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The outer shell and supporting panels look intentional, cohesive, and keyboard-friendly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify responsive layout and polish in the browser</name>
  <files>test/standalone.html</files>
  <read_first>
    - src/body-map-explorer.ts
    - src/body-map-sidebar.ts
    - src/body-map-detail-panel.ts
    - src/body-map-data-panel.ts
    - test/standalone.html
  </read_first>
  <action>
    Verify the responsive contract in a browser:
    1. Start the dev server with `npm run dev`.
    2. Open the standalone page or host page.
    3. Resize the component through wide, tablet-like, and narrow widths.
    4. Confirm the layout reflows without horizontal overflow and that sticky behavior does not trap the data panel in narrow mode.
    5. Confirm focus rings, hover states, and selected states look deliberate rather than default.
  </action>
  <acceptance_criteria>
    - The explorer can be embedded at narrow widths without horizontal scrolling.
    - The model remains readable and reachable in all container widths.
    - The polished states look consistent across sidebar, detail panel, and data panel.
  </acceptance_criteria>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host container width -> component layout | External embed width changes the internal panel arrangement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial of Service | responsive shell | mitigate | Use explicit container breakpoints and disable sticky/max-height traps in narrow layouts so content remains reachable. |
</threat_model>

<verification>
1. `npm test`
2. Manual browser resizing at multiple embed widths
</verification>

<success_criteria>
1. The component layout responds to its own width using container queries.
2. The component has a cohesive, polished visual language across panels and controls.
3. Narrow embeds remain usable without horizontal overflow or trapped sticky panels.
</success_criteria>

<output>
After completion, create .planning/phases/06-polish-back-view-performance/06-02-SUMMARY.md
</output>
