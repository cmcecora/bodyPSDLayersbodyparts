---
phase: "06-polish-back-view-performance"
plan: "03"
type: "execute"
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/body-map-model.ts
  - src/__tests__/body-map-model.test.ts
autonomous: false
requirements:
  - BACK-01
  - BACK-02
  - BACK-03

must_haves:
  truths:
    - "Body sections mode can switch between front and back with a CSS 3D flip."
    - "Female and male back-view assets both render correctly in sections mode."
    - "Only the active face participates in click and keyboard interaction during the flip."
  artifacts:
    - path: "src/body-map-model.ts"
      provides: "Front/back scene, asset switching, and interaction-safe flip behavior"
    - path: "src/__tests__/body-map-model.test.ts"
      provides: "Automated verification for front/back assets and flip state"
  key_links:
    - from: "_sectionsFacing"
      to: "rendered front/back face"
      via: "scene transform and asset selection"
---

<objective>
Turn the current front/back toggle into a real body-sections flip experience using the back-view assets already present in the repo. This plan focuses on the model scene itself so the final result feels intentional and interactive, not like a plain asset swap.

Purpose: deliver BACK-01, BACK-02, and BACK-03 with a clear separation between front and back faces and tests that prove the correct assets are used for both male and female states.
Output: a flip-capable sections scene in `body-map-model`, updated tests for front/back assets and state, and a manual visual verification checkpoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-back-view-performance/06-RESEARCH.md

@src/body-map-model.ts
@src/data/sections.ts
@src/__tests__/body-map-model.test.ts

<assets>
- public/assets/sections-body.webp
- public/assets/sections-body-back.webp
- public/assets/sections-body-male.webp
- public/assets/sections-body-male-back.webp
</assets>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a 3D front/back sections scene instead of a plain asset swap</name>
  <files>src/body-map-model.ts, src/__tests__/body-map-model.test.ts</files>
  <read_first>
    - src/body-map-model.ts
    - src/data/sections.ts
    - src/__tests__/body-map-model.test.ts
  </read_first>
  <action>
    Rework sections mode so the front/back toggle is rendered as a scene with explicit faces:
    1. In `src/body-map-model.ts`, create a dedicated sections-only scene wrapper (for example `.flip-scene` / `.flip-card`) that is rendered only when `currentView === "sections"`.
    2. Render a front face and back face separately instead of a single sections asset swap. The back face should be rotated `180deg` and hidden with `backface-visibility: hidden`.
    3. Apply the scene transform from `_sectionsFacing` so the toggle animates with `transform: rotateY(180deg)` and a timed transition rather than a jump-cut.
    4. Ensure the inactive face has pointer events disabled and cannot become the keyboard target while hidden.
    5. Update the rotate button so it exposes `aria-pressed` and an exact label describing the next action (`View Back` / `View Front`).
    6. Extend `src/__tests__/body-map-model.test.ts` to assert that sections mode includes flip-scene state classes or transform strings and that toggling the button changes the facing state.
  </action>
  <acceptance_criteria>
    - `src/body-map-model.ts` contains a scene/faces structure for sections mode, not just a single conditional asset swap.
    - `src/body-map-model.ts` contains `backface-visibility` in the sections-face styles.
    - `src/body-map-model.ts` contains a `rotateY(180deg)` transform for the back-facing state.
    - `src/__tests__/body-map-model.test.ts` contains an assertion against the flip-state class or transform after clicking the rotate button.
    - `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Sections mode flips between explicit front and back faces with safe interaction behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the correct female and male back-view assets into the flipped sections faces</name>
  <files>src/body-map-model.ts, src/__tests__/body-map-model.test.ts</files>
  <read_first>
    - src/body-map-model.ts
    - src/data/sections.ts
    - src/__tests__/body-map-model.test.ts
  </read_first>
  <action>
    Make the flip scene render the right asset/section set for each gender:
    1. Use `public/assets/sections-body-back.webp` as the female back asset and `public/assets/sections-body-male-back.webp` as the male back asset.
    2. Keep the existing female/male front assets for the front face and keep the current-view logic isolated to sections mode.
    3. Render the front face with the correct front sections filtered by gender and the back face with the shared `side === "back"` sections.
    4. Ensure the `viewBox` logic remains correct for male front/back and female front/back states so the asset frame and section hit areas stay aligned.
    5. Extend `src/__tests__/body-map-model.test.ts` with explicit assertions for all four section base asset cases: female front, female back, male front, male back.
  </action>
  <acceptance_criteria>
    - `src/body-map-model.ts` contains both `sections-body-back.webp` and `sections-body-male-back.webp` in its asset-selection flow.
    - `src/__tests__/body-map-model.test.ts` contains explicit assertions for female back and male back asset hrefs.
    - `src/__tests__/body-map-model.test.ts` still verifies section counts for visible front/back states.
    - `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Front/back scenes use the right artwork and section sets for both male and female modes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify front/back flip in the browser</name>
  <files>test/standalone.html</files>
  <read_first>
    - src/body-map-model.ts
    - test/standalone.html
  </read_first>
  <action>
    Verify the visual flip behavior in the browser:
    1. Start the dev server with `npm run dev`.
    2. Switch the model to Body Sections.
    3. Toggle between front and back for female and male.
    4. Confirm the flip animates smoothly and the hidden face cannot be clicked or focused.
  </action>
  <acceptance_criteria>
    - Female sections render the green back-body asset when flipped to back.
    - Male sections render the male back-body asset when flipped to back.
    - The transition reads as a flip, not a flash or hard swap.
  </acceptance_criteria>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Facing state -> active scene face | UI state determines which face can receive pointer and keyboard interaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Spoofing / UX integrity | front/back scene | mitigate | Disable pointer/focus on the hidden face and test the flip state explicitly. |
</threat_model>

<verification>
1. `npm test`
2. Manual browser verification of front/back flip for male and female
</verification>

<success_criteria>
1. Sections mode supports a 3D flip between front and back.
2. The correct back-view asset renders for both female and male modes.
3. Hidden flip faces do not intercept interaction.
</success_criteria>

<output>
After completion, create .planning/phases/06-polish-back-view-performance/06-03-SUMMARY.md
</output>
