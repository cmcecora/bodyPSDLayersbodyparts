---
phase: "06-polish-back-view-performance"
plan: "04"
type: "execute"
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - src/body-map-explorer.ts
  - src/body-map-model.ts
  - src/body-map-detail-panel.ts
  - src/data/body-parts.ts
  - src/data/systems.ts
  - src/__tests__/body-map-model.test.ts
  - src/__tests__/body-map-explorer.test.ts
  - scripts/check-build-budget.js
  - package.json
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "Hot interaction paths avoid repeated DOM queries and repeated linear data lookups."
    - "Non-critical assets are deferred so inactive views/faces do not load on first paint."
    - "The project has an explicit build-budget check for the component shell and critical initial assets."
  artifacts:
    - path: "src/body-map-explorer.ts"
      provides: "Cached refs/maps for explorer hot paths"
    - path: "src/body-map-model.ts"
      provides: "Deferred non-critical SVG asset references"
    - path: "src/body-map-detail-panel.ts"
      provides: "Native lazy-loading for photo content below the primary view"
    - path: "scripts/check-build-budget.js"
      provides: "Repeatable budget verification for build output"
    - path: "package.json"
      provides: "Runnable budget-check script"
  key_links:
    - from: "build output"
      to: "budget check script"
      via: "npm script"
    - from: "explorer/model hot paths"
      to: "cached refs and lookup maps"
      via: "precomputed state"
---

<objective>
Close the phase with concrete performance work: cache read-mostly lookups, stop loading non-critical assets on first paint, and add a repeatable build-budget check that can prove the component remains under the required load budget.

Purpose: deliver PERF-01, PERF-02, and PERF-03 with measurable output instead of subjective “it feels faster” changes.
Output: cached lookup paths, deferred asset loading, lazy photo rendering where native support exists, and a committed build-budget script.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-back-view-performance/06-RESEARCH.md

@src/body-map-explorer.ts
@src/body-map-model.ts
@src/body-map-detail-panel.ts
@src/data/body-parts.ts
@src/data/systems.ts
@src/__tests__/body-map-model.test.ts
@src/__tests__/body-map-explorer.test.ts
@package.json
@vite.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache repeated DOM and data lookups in explorer/model hot paths</name>
  <files>src/body-map-explorer.ts, src/data/body-parts.ts, src/data/systems.ts, src/__tests__/body-map-explorer.test.ts</files>
  <read_first>
    - src/body-map-explorer.ts
    - src/data/body-parts.ts
    - src/data/systems.ts
    - src/__tests__/body-map-explorer.test.ts
  </read_first>
  <action>
    Replace repeated lookup/query patterns with concrete cached structures:
    1. Export `BODY_PARTS_BY_ID` from `src/data/body-parts.ts` and `BODY_SYSTEMS_BY_ID` from `src/data/systems.ts` using `new Map(...)`.
    2. Update `src/body-map-explorer.ts` to use those maps instead of repeated `BODY_PARTS.find(...)` and `BODY_SYSTEMS.find(...)` in selection and detail-photo flows.
    3. Replace `this.renderRoot.querySelector("body-map-model")` in `_resolveOrgans2ModalAnchor` with a cached element reference (for example a `@query("body-map-model")` field or a private ref set during render/update).
    4. Add tests in `src/__tests__/body-map-explorer.test.ts` that assert the exported maps exist and that the explorer still resolves selection/detail state correctly after the refactor.
  </action>
  <acceptance_criteria>
    - `src/data/body-parts.ts` contains `BODY_PARTS_BY_ID`.
    - `src/data/systems.ts` contains `BODY_SYSTEMS_BY_ID`.
    - `src/body-map-explorer.ts` no longer contains `renderRoot.querySelector("body-map-model")`.
    - `src/body-map-explorer.ts` uses the exported lookup maps in place of repeated `.find(` calls for body-part/system resolution.
    - `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Explorer and model hot paths use cached refs/maps instead of repeated DOM and array scans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Defer non-critical assets and enable native lazy loading where available</name>
  <files>src/body-map-model.ts, src/body-map-detail-panel.ts, src/__tests__/body-map-model.test.ts, src/__tests__/body-map-explorer.test.ts</files>
  <read_first>
    - src/body-map-model.ts
    - src/body-map-detail-panel.ts
    - src/__tests__/body-map-model.test.ts
    - src/__tests__/body-map-explorer.test.ts
  </read_first>
  <action>
    Defer assets in concrete ways the browser can actually honor:
    1. In `src/body-map-model.ts`, do not render inactive section-face asset references until sections mode is active and the corresponding face is needed. Hidden/inactive front/back assets must be omitted from the DOM, not merely hidden by opacity.
    2. Keep organ-view asset rendering limited to the currently active view/gender path so inactive view assets are not eagerly referenced.
    3. In `src/body-map-detail-panel.ts`, add `loading="lazy"` and `decoding="async"` to body-part photo `<img>` elements, and apply the same to the system thumbnail if it can appear below the fold.
    4. Add tests that verify hidden face asset refs are absent when not needed and that detail-panel images render with `loading="lazy"`.
  </action>
  <acceptance_criteria>
    - `src/body-map-model.ts` omits inactive face asset refs from the DOM instead of hiding them with opacity alone.
    - `src/body-map-detail-panel.ts` contains `loading="lazy"` and `decoding="async"` on photo images.
    - `src/__tests__/body-map-model.test.ts` contains an assertion that inactive face asset refs are absent in the initial state.
    - `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Only critical initial assets are referenced on first paint, and below-fold image content uses native lazy loading where supported.</done>
</task>

<task type="auto">
  <name>Task 3: Add a repeatable build-budget check for the component shell and critical initial assets</name>
  <files>scripts/check-build-budget.js, package.json</files>
  <read_first>
    - package.json
    - vite.config.ts
    - dist/ (after `npm run build`)
  </read_first>
  <action>
    Add a concrete build-budget verification script:
    1. Create `scripts/check-build-budget.js` that reads file sizes for `dist/body-map-explorer.es.js` and the critical initial asset set used by the default first render (for example `dist/assets/silhouette.webp` and the default current-view body asset if it is eagerly loaded).
    2. Print a size breakdown and exit non-zero if the summed critical payload exceeds `512000` bytes.
    3. Add `"check:budget": "node scripts/check-build-budget.js"` to `package.json`.
    4. Document the expected command in the verification section and include it in the plan summary after execution.
  </action>
  <acceptance_criteria>
    - `scripts/check-build-budget.js` exists.
    - `scripts/check-build-budget.js` contains `512000` as the failing threshold.
    - `package.json` contains `"check:budget": "node scripts/check-build-budget.js"`.
    - Running `npm run build && npm run check:budget` exits 0 once the performance work is complete.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run check:budget</automated>
  </verify>
  <done>The repo has a repeatable, failing budget check instead of a one-off manual size estimate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify initial network budget and deferred assets in the browser</name>
  <files>test/standalone.html</files>
  <read_first>
    - src/body-map-model.ts
    - src/body-map-detail-panel.ts
    - scripts/check-build-budget.js
    - test/standalone.html
  </read_first>
  <action>
    Confirm the performance claims with real browser behavior:
    1. Run `npm run build && npm run check:budget`.
    2. Start the app and open DevTools Network.
    3. Hard-refresh the default view.
    4. Verify inactive front/back assets are not fetched on first paint.
    5. Scroll or interact to the detail-photo area and verify those images lazy-load rather than all starting immediately.
  </action>
  <acceptance_criteria>
    - The budget script passes.
    - Inactive view/facing assets are not requested during the initial page load.
    - Detail/photo imagery requests can be observed loading later than the initial shell.
  </acceptance_criteria>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output -> release acceptance | The repo must reject builds whose critical initial payload exceeds the budget. |
| Deferred assets -> user-visible UI | Assets withheld from first paint must still load when the corresponding UI becomes active. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Denial of Service | oversized initial payload | mitigate | Add a hard failing budget script with a 512000-byte threshold. |
| T-06-06 | Tampering / UX integrity | deferred assets | mitigate | Verify hidden assets are omitted initially but requested when their face/view/photo becomes active. |
</threat_model>

<verification>
1. `npm test`
2. `npm run build && npm run check:budget`
3. Manual browser network verification
</verification>

<success_criteria>
1. Hot interaction paths avoid repeated DOM queries and repeated array scans.
2. Initial render references only the critical assets required for the active first view.
3. The project has an explicit build-budget check that enforces the 500 KB target.
</success_criteria>

<output>
After completion, create .planning/phases/06-polish-back-view-performance/06-04-SUMMARY.md
</output>
