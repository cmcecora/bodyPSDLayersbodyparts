---
phase: 03-body-systems-sidebar-detail-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/systems.ts
  - src/body-map-sidebar.ts
  - src/body-map-detail-panel.ts
  - src/__tests__/systems-data.test.ts
  - src/__tests__/body-systems-panels.test.ts
autonomous: true
requirements:
  [SYSTEM-01, SYSTEM-04]

must_haves:
  truths:
    - "Users can see all 11 body systems in the left sidebar with a color dot, thumbnail, and title for each row"
    - "The right detail panel has an explicit empty state before any system is selected"
    - "A selected system can be rendered with its title, thumbnail, and description without explorer-specific logic"
  artifacts:
    - path: "src/data/systems.ts"
      provides: "Canonical typed body-system dataset and organ reverse lookup"
      exports: ["BodySystemId", "BodySystemDefinition", "BODY_SYSTEMS", "ORGAN_TO_SYSTEM"]
    - path: "src/body-map-sidebar.ts"
      provides: "<body-map-sidebar> presentational sidebar component"
      exports: ["BodyMapSidebar"]
    - path: "src/body-map-detail-panel.ts"
      provides: "<body-map-detail-panel> presentational detail renderer"
      exports: ["BodyMapDetailPanel"]
    - path: "src/__tests__/body-systems-panels.test.ts"
      provides: "Component tests for sidebar rendering, events, and detail-panel states"
      min_lines: 40
  key_links:
    - from: "src/body-map-sidebar.ts"
      to: "src/data/systems.ts"
      via: "systems property and row rendering"
      pattern: "BODY_SYSTEMS|BodySystemDefinition"
    - from: "src/body-map-detail-panel.ts"
      to: "src/data/systems.ts"
      via: "system property typed as BodySystemDefinition | null"
      pattern: "BodySystemDefinition"
    - from: "src/__tests__/systems-data.test.ts"
      to: "src/data/organs.ts"
      via: "cross-reference of mapped organ ids against ORGANS"
      pattern: "import.*ORGANS.*from"
---

<objective>
Create the Phase 03 source-of-truth dataset and presentational sidebar/detail components that explorer wiring can build on.

Purpose: Phase 03 needs stable contracts before cross-component coordination begins. This plan defines the 11 systems, their organ mappings, and the two new UI components so the wiring plan can focus on state flow instead of rebuilding data and rendering concerns.

Output: `src/data/systems.ts`, `src/body-map-sidebar.ts`, `src/body-map-detail-panel.ts`, and focused Vitest coverage for the new contracts.
</objective>

<execution_context>
@/Users/chriscecora/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chriscecora/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-body-systems-sidebar-detail-panel/03-RESEARCH.md
@.planning/phases/02-core-svg-body-model/02-02-SUMMARY.md
@interactive-body-model-app.js
@src/body-map-explorer.ts
@src/data/organs.ts
@src/styles/tokens.css.ts

<interfaces>
From `src/data/organs.ts`:

```typescript
export interface OrganDefinition {
  id: string;
  name: string;
  hitAreaPath: string;
  imageX: number;
  imageY: number;
  imageWidth: number;
  imageHeight: number;
  isMaleRepro?: boolean;
  isFemaleRepro?: boolean;
}

export const ORGANS: OrganDefinition[];
```

From `src/body-map-explorer.ts`:

```typescript
@customElement("body-map-explorer")
export class BodyMapExplorer extends LitElement {
  // Current placeholder layout: 260px sidebar | 1fr center | 300px detail
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the canonical systems data module and reverse organ lookup</name>
  <files>src/data/systems.ts, src/__tests__/systems-data.test.ts</files>
  <behavior>
    - Test 1: `BODY_SYSTEMS` exposes exactly 11 ids in this order: `cardiovascular`, `digestive`, `endocrine`, `immune`, `integumentary`, `muscular`, `nervous`, `reproductive`, `respiratory`, `skeletal`, `urinary`.
    - Test 2: Every `organIds` entry exists in `ORGANS`, and the empty `integumentary` mapping stays empty instead of using placeholder ids.
    - Test 3: `ORGAN_TO_SYSTEM.heart` is `["cardiovascular"]`, `ORGAN_TO_SYSTEM.thymus` is `["endocrine", "immune"]`, `ORGAN_TO_SYSTEM.knee_joint` is `["muscular", "skeletal"]`, and both reproductive ids map to `["reproductive"]`.
  </behavior>
  <read_first>
    - src/data/organs.ts
    - interactive-body-model-app.js (the `BODY_SYSTEMS` block and `ORGAN_TO_SYSTEM` block near the top of the file)
    - public/assets/systems/ directory contents
    - .planning/phases/03-body-systems-sidebar-detail-panel/03-RESEARCH.md
  </read_first>
  <action>
Create `src/data/systems.ts` with these exported contracts:

```typescript
export type BodySystemId =
  | "cardiovascular"
  | "digestive"
  | "endocrine"
  | "immune"
  | "integumentary"
  | "muscular"
  | "nervous"
  | "reproductive"
  | "respiratory"
  | "skeletal"
  | "urinary";

export interface BodySystemDefinition {
  id: BodySystemId;
  title: string;
  color: string;
  thumbnail: string;
  description: string;
  organIds: string[];
}
```

Populate `BODY_SYSTEMS` with these exact ids, colors, thumbnail paths, and organ mappings:

- `cardiovascular` / `Cardiovascular` / `#e87722` / `"/assets/systems/cardiovascular.webp"` / `["heart"]`
- `digestive` / `Digestive` / `#8061bc` / `"/assets/systems/digestive.webp"` / `["stomach", "intestines", "gallbladder", "liver"]`
- `endocrine` / `Endocrine` / `#a32a2e` / `"/assets/systems/endocrine.webp"` / `["thyroid", "pancreas", "thymus"]`
- `immune` / `Immune` / `#b1b3b3` / `"/assets/systems/immune.webp"` / `["spleen", "thymus"]`
- `integumentary` / `Integumentary` / `#f2b411` / `"/assets/systems/integumentary.webp"` / `[]`
- `muscular` / `Muscular` / `#078576` / `"/assets/systems/muscular.webp"` / `["muscle", "knee_joint"]`
- `nervous` / `Nervous` / `#627d32` / `"/assets/systems/nervous.webp"` / `["brain"]`
- `reproductive` / `Reproductive` / `#005e9d` / `"/assets/systems/reproductive.webp"` / `["male_reproductive", "female_reproductive"]`
- `respiratory` / `Respiratory` / `#422c88` / `"/assets/systems/respiratory.webp"` / `["lungs_left", "lungs_right", "larynx_trachea"]`
- `skeletal` / `Skeletal` / `#90bc53` / `"/assets/systems/skeletal.webp"` / `["knee_joint"]`
- `urinary` / `Urinary` / `#e87722` / `"/assets/systems/urinary.webp"` / `["kidneys", "bladder"]`

Port the `description` string for each of those 11 systems from the matching entry in `interactive-body-model-app.js`. Do not copy `keyParts`, `processes`, or the legacy `other` entry.

Export `ORGAN_TO_SYSTEM` by reducing `BODY_SYSTEMS` into a reverse lookup. Use immutable array pushes (`lookup[organId] = [...(lookup[organId] ?? []), system.id]`) or a clear typed equivalent; do not hardcode the reverse map separately.

Create `src/__tests__/systems-data.test.ts` before implementation assertions are finalized. The test file must import `BODY_SYSTEMS`, `ORGAN_TO_SYSTEM`, and `ORGANS`, and assert the exact mappings listed in the behavior block.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/systems-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/systems.ts` contains `export type BodySystemId =`
    - `src/data/systems.ts` contains `export interface BodySystemDefinition`
    - `src/data/systems.ts` contains `export const BODY_SYSTEMS`
    - `src/data/systems.ts` contains `thumbnail: "/assets/systems/cardiovascular.webp"` and `thumbnail: "/assets/systems/urinary.webp"`
    - `src/data/systems.ts` contains `organIds: ["male_reproductive", "female_reproductive"]`
    - `src/data/systems.ts` contains `export const ORGAN_TO_SYSTEM`
    - `src/__tests__/systems-data.test.ts` contains `expect(BODY_SYSTEMS).toHaveLength(11)`
    - `src/__tests__/systems-data.test.ts` contains `expect(ORGAN_TO_SYSTEM.thymus).toEqual(["endocrine", "immune"])`
    - `src/__tests__/systems-data.test.ts` contains `expect(ORGAN_TO_SYSTEM.knee_joint).toEqual(["muscular", "skeletal"])`
  </acceptance_criteria>
  <done>`systems.ts` is the single source of truth for Phase 03 system ids, metadata, and organ mappings, and the data contract is covered by executable tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build sidebar and detail-panel components against the new system contracts</name>
  <files>src/body-map-sidebar.ts, src/body-map-detail-panel.ts, src/__tests__/body-systems-panels.test.ts</files>
  <behavior>
    - Test 1: `<body-map-sidebar>` renders 11 `button[type="button"]` rows from `BODY_SYSTEMS`, each row containing a `.system-dot`, a `.system-thumb`, and the system title text.
    - Test 2: Clicking a sidebar row dispatches `system-toggle-request` with `{ systemId }`, and the event bubbles and is composed.
    - Test 3: `<body-map-detail-panel>` renders the exact empty-state copy `Select a body system to see details.` when `system === null`, and renders the selected system title, description, and thumbnail when `system` is provided.
  </behavior>
  <read_first>
    - src/body-map-explorer.ts
    - src/styles/tokens.css.ts
    - src/data/systems.ts
    - .planning/phases/03-body-systems-sidebar-detail-panel/03-RESEARCH.md
  </read_first>
  <action>
Create `src/body-map-sidebar.ts` as a presentational Lit component that:

- imports `BODY_SYSTEMS`, `BodySystemDefinition`, and `BodySystemId` from `./data/systems.js`
- exposes `@property({ attribute: false }) systems: BodySystemDefinition[] = BODY_SYSTEMS`
- exposes `@property({ type: String }) activeSystemId: BodySystemId | null = null`
- renders a panel header with the exact text `Body Systems`
- renders each row as `<button type="button" class="system-button">`
- includes `data-system-id=${system.id}` on each button
- sets `aria-pressed=${String(system.id === this.activeSystemId)}`
- dispatches `new CustomEvent("system-toggle-request", { detail: { systemId: system.id }, bubbles: true, composed: true })`

Use `designTokens` for base panel colors and spacing. Add `.active` styling on the button or list item, but keep the event behavior in the component and keep all selection state external.

Create `src/body-map-detail-panel.ts` as a presentational Lit component that:

- imports `BodySystemDefinition` from `./data/systems.js`
- exposes `@property({ attribute: false }) system: BodySystemDefinition | null = null`
- renders a panel header with the exact text `Detail Panel`
- renders the empty-state copy `Select a body system to see details.` when `system` is `null`
- renders `<img class="detail-thumb" src=${system.thumbnail} alt=${system.title}>`, `<h3 class="detail-title">${system.title}</h3>`, and `<p class="detail-description">${system.description}</p>` when `system` is set

Create `src/__tests__/body-systems-panels.test.ts` covering the behavior above. Use real component fixtures rather than DOM stubs so the tests prove the rendered markup and event payloads.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/systems-data.test.ts src/__tests__/body-systems-panels.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/body-map-sidebar.ts` contains `@customElement("body-map-sidebar")`
    - `src/body-map-sidebar.ts` contains `system-toggle-request`
    - `src/body-map-sidebar.ts` contains `button type="button"`
    - `src/body-map-sidebar.ts` contains `data-system-id=`
    - `src/body-map-detail-panel.ts` contains `@customElement("body-map-detail-panel")`
    - `src/body-map-detail-panel.ts` contains `Select a body system to see details.`
    - `src/body-map-detail-panel.ts` contains `class="detail-thumb"`
    - `src/__tests__/body-systems-panels.test.ts` contains `expect(buttons).toHaveLength(11)`
    - `src/__tests__/body-systems-panels.test.ts` contains `expect(event?.detail).toEqual({ systemId: "cardiovascular" })`
    - `src/__tests__/body-systems-panels.test.ts` contains `expect(detail.shadowRoot?.textContent).toContain("Select a body system to see details.")`
  </acceptance_criteria>
  <done>The sidebar and detail panel exist as reusable components with explicit empty/selected states and executable tests proving their contract.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/__tests__/systems-data.test.ts src/__tests__/body-systems-panels.test.ts` and confirm both the data contract and the presentational component contract pass before moving to Plan 03-02.
</verification>

<success_criteria>
Phase 03 has a typed systems module and two reusable panel components ready for coordinator wiring, with no placeholder ids, no DOM scraping, and no missing empty-state behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-body-systems-sidebar-detail-panel/03-01-SUMMARY.md`
</output>
