---
phase: "05-web-component-api"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["src/body-map-explorer.ts", "src/body-map-explorer.test.ts", "test/standalone.html"]
autonomous: false
requirements: ["API-02", "API-03", "API-04", "API-05"]

must_haves:
  truths:
    - "Host can set selected-organ-ids attribute to highlight organs"
    - "Component dispatches organ-selected event when an organ is clicked"
    - "Component dispatches system-selected event when a system is toggled"
    - "Setting asset-base attribute prefixes all asset and data URLs"
  artifacts:
    - path: "src/body-map-explorer.ts"
      provides: "Public API with @property and CustomEvents"
    - path: "src/body-map-explorer.test.ts"
      provides: "Automated verification of API and events"
    - path: "test/standalone.html"
      provides: "Manual verification playground for library mode"
  key_links:
    - from: "selectedOrganIds property"
      to: "body-map-model.selectedOrganIds"
      via: "Lit property binding"
    - from: "internal click handler"
      to: "CustomEvent dispatch"
      via: "this.dispatchEvent()"
---

<objective>
Transform the &lt;body-map-explorer&gt; into a formal Web Component with a public API. This enables the component to be used as a standalone library where host applications can programmatically control selection state, respond to user interactions via events, and customize asset loading locations.

Purpose: Decouple the component from the specific demo environment and make it a reusable product.
Output: Refactored orchestrator component, comprehensive test suite, and a standalone HTML testbed.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-component-api/05-RESEARCH.md
@.planning/phases/05-web-component-api/05-CONTEXT.md
@src/body-map-explorer.ts

<interfaces>
From src/data/systems.ts:
```typescript
export type BodySystemId = string;
export interface BodySystemDefinition {
  id: BodySystemId;
  name: string;
  organIds: string[];
  detailBodyPartIds?: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Public Properties and Events in &lt;body-map-explorer&gt;</name>
  <files>src/body-map-explorer.ts</files>
  <behavior>
    - Property 'selectedOrganIds' reflects to 'selected-organ-ids' attribute (comma-separated).
    - Property 'activeSystemId' reflects to 'active-system-id' attribute.
    - Component dispatches 'organ-selected' (detail: { organId }) and 'organ-deselected' (detail: { organId }) events.
    - Component dispatches 'system-selected' (detail: { systemId, active: boolean }) events.
  </behavior>
  <action>
    1. Replace '@state() private activeSystemId' with '@property({ type: String, attribute: "active-system-id", reflect: true }) activeSystemId: BodySystemId | null = null;'.
    2. Replace '@state() private selectedOrganIds' with '@property' and implement a converter for string[] to/from comma-separated attribute.
       ```typescript
       @property({
         type: Array,
         attribute: "selected-organ-ids",
         reflect: true,
         converter: {
           fromAttribute: (value: string) => value ? value.split(",") : [],
           toAttribute: (value: string[]) => value.join(",")
         }
       })
       selectedOrganIds: string[] = [];
       ```
    3. Update '_handleOrganSelectRequest' and '_handleOrganSelectionChange' to dispatch 'organ-selected' or 'organ-deselected' events.
    4. Update '_handleSystemToggleRequest' to dispatch 'system-selected' event.
    5. Ensure 'assetBase' is correctly propagated to all sub-components (sidebar, model, detail-panel).
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create src/body-map-explorer.test.ts first</automated>
  </verify>
  <done>Properties reflect correctly to attributes and events are dispatched with proper detail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add API tests for &lt;body-map-explorer&gt;</name>
  <files>src/body-map-explorer.test.ts</files>
  <behavior>
    - Setting 'selected-organ-ids' attribute updates 'selectedOrganIds' property and highlights organs.
    - Clicking an organ in the model dispatches 'organ-selected' event.
    - Programmatically setting 'activeSystemId' property updates the active system in the sidebar.
    - Changing 'asset-base' updates image source paths.
  </behavior>
  <action>
    Create 'src/body-map-explorer.test.ts' using '@testing-library/dom' or simple 'document.createElement' with Vitest.
    - Test Case 1: Attribute reflection for selection and system.
    - Test Case 2: Event capturing for organ and system selection.
    - Test Case 3: Asset base URL construction verification (check rendered img src or pass to sub-components).
  </action>
  <verify>
    <automated>npx vitest run src/body-map-explorer.test.ts</automated>
  </verify>
  <done>All API and event tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create Standalone HTML Testbed</name>
  <files>test/standalone.html</files>
  <action>
    1. Create a directory 'test/' if it doesn't exist.
    2. Create 'test/standalone.html' containing:
       - A &lt;script type="module" src="../src/body-map-explorer.ts"&gt;&lt;/script&gt; (Vite handles the transformation during dev).
       - An instance of &lt;body-map-explorer id="explorer"&gt;&lt;/body-map-explorer&gt;.
       - UI controls (input + buttons) to set attributes programmatically.
       - An event log area that displays captured CustomEvents from the explorer.
  </action>
  <verify>
    <automated>ls test/standalone.html</automated>
  </verify>
  <done>Standalone testbed exists and provides a way to verify the component API in isolation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Web Component API</name>
  <action>Verify the public API and events manually in the standalone testbed.</action>
  <what-built>Refactored &lt;body-map-explorer&gt; with Public API and events.</what-built>
  <how-to-verify>
    1. Run 'npm run dev'.
    2. Open 'http://localhost:5173/test/standalone.html' (or the correct path in your dev server).
    3. Type an organ ID (e.g., 'heart') into the input and click 'Set Selected'. Verify the heart highlights.
    4. Click an organ on the model. Verify an event appears in the 'Event Log' on the page.
    5. Toggle a system in the sidebar. Verify the 'system-selected' event appears in the log.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Attribute → Component | Untrusted string input from host HTML attribute enters component logic. |
| Event → Host App | Component sends data (IDs, names) back to the host application. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | asset-base attribute | mitigate | Sanitize 'asset-base' input to ensure it is a valid URL or path prefix, preventing injection of malicious script tags or unexpected URL schemes. |
| T-05-02 | Information Disclosure | CustomEvents | accept | Event detail only contains IDs and names which are already public in the data files. No PII or sensitive state is exposed. |
</threat_model>

<verification>
1. All Vitest unit tests in 'src/body-map-explorer.test.ts' pass.
2. Manual verification in 'test/standalone.html' confirms bidirectional API flow.
</verification>

<success_criteria>
1. &lt;body-map-explorer&gt; supports 'selected-organ-ids', 'active-system-id', and 'asset-base' attributes.
2. Events 'organ-selected', 'organ-deselected', and 'system-selected' are dispatched with correct details.
3. Component correctly re-renders when attributes are changed via the browser console or host script.
</success_criteria>

<output>
After completion, create .planning/phases/05-web-component-api/05-01-SUMMARY.md
</output>
